<div class="menu">
    
    <table>
        <tbody>
            <tr>
                <td><div class="@(Model.CurrentTab == 1 ? "highlight" : "")">CMDR</div></td>
            </tr>
            <tr>
                <td><div class="@(Model.CurrentTab == 2 ? "highlight" : "")">RANK</div></td>
            </tr>
            <tr>
                <td><div class="@(Model.CurrentTab == 3 ? "highlight" : "")">SHIP</div></td>
            </tr>
            <tr>
                <td><div class="@(Model.CurrentTab == 4 ? "highlight" : "")">NAV</div></td>
            </tr>
            <tr>
                <td><div class="@(Model.CurrentTab == 5 ? "highlight" : "")">EVENT</div></td>
            </tr>
            <tr>
                <td><div>&nbsp;</div></td>
            </tr>
        </tbody>
    </table>
</div>
